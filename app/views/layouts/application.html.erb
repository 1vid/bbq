<!DOCTYPE html>
<html>
  <head>
    <title><%= t('title') %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_include_tag "https://api-maps.yandex.ru/2.1/?lang=ru_RU&apikey=#{ Rails.application.credentials.ym_api_key }" %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <%= render 'partials/navigation' %>
    <div class="container">
      <% if notice %>
        <div class="alert alert-info mt-3 alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>
    <footer class="footer mt-auto py-3 bg-light">
      <div class="container text-muted">
        <span >&copy; <%= t('company') %></span> <%= Time.current.year %>
      </div>
    </footer>
  </body>
</html>
